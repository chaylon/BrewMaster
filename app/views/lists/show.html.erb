<%= @list.name %><br/>
<%= @list.description %>

<!-- <% unless @beers.empty? %>
  <ul>
    <% @beers.each do |beer| %>
      <li><%= beer.name %></li>
    <% end %>
  </ul>
<% end %> -->

<div id="ListShow"/>

<%= link_to "Find new beers!", beers_path %>
