<div class="list-header">
  <h3><%= @list.name %></h3>
  <% if @list.description != "" %>
    <h4><%= @list.description %></h4>
  <% end %>
  -<%= link_to "#{@list.user.first_name} #{@list.user.last_name}", user_path(@list.user) %></br>
  <% if @list.user == current_user %>
  <%= link_to "Edit", edit_list_path(@list) %> |
  <%= link_to "Delete", @list, method: :delete, data: {confirm: "Are you sure?"} %></br>
  <% end %>
</div>

</br>

<div id="ListShow"/>

<%= link_to "Find new beers!", beers_path %>
